services:
  frontend:
    #build:
    #  context: .
    #  dockerfile: Dockerfile
    #image: elines-krudtugler-frontend:latest
    image: ghcr.io/emilstorgaard/elines-krudtugler-frontend:latest
    container_name: elines_krudtugler_frontend
    ports:
      - '8480:3000'
    restart: unless-stopped
    env_file:
      - .env